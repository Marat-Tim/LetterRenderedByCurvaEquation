// nerdamer подключён глобально через <script> в index.html

export const eps = '1/10';

export function sqrt(expr) {
    return `sqrt(${expr})`;
}

export function div(a, b) {
    return `(${a})/(${b})`;
}

export function abs(expr) {
    return `abs(${expr})`;
}
